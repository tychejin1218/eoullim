{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | grep -E '\\.(ts|tsx|css|json)$' | xargs -r npx prettier --write 2>/dev/null || true",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | grep -E '\\.(ts|tsx)$' | xargs -r npx eslint --fix 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
